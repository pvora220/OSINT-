version: '3.8'

services:
  matrix-osint:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: matrix-osint
    ports:
      - "3000:3000"
    volumes:
      - ../data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
